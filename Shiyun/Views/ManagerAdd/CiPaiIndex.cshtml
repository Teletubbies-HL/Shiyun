@model IEnumerable<Models.CiPai>

@{
    ViewBag.Title = "CiPaiIndex";
}

<h2>CiPaiIndex</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CiPaiName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CiPaiJieshao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CiPaiName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CiPaiJieshao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CiPai_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.CiPai_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CiPai_id })
        </td>
    </tr>
}

</table>
